# ix-storage-helixdb Tasks

- [ ] Define concrete schema mappings (nodes, edges, vectors, provenance)
- [ ] Implement CRUD + traversal + vector search
- [ ] Add migration/versioning strategy for cache rebuilds
