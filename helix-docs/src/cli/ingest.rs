use crate::error::Result;

pub fn run(force: bool, concurrency: usize, embed: bool, _json: bool) -> Result<()> {
    todo!("ingest command: force={force}, concurrency={concurrency}, embed={embed}")
}
