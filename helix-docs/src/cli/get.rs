use crate::error::Result;

pub fn run(
    library: Option<String>,
    path: Option<String>,
    doc: Option<String>,
    _slice: Option<String>,
    _raw: bool,
    _json: bool,
) -> Result<()> {
    todo!("get command: library={library:?}, path={path:?}, doc={doc:?}")
}
