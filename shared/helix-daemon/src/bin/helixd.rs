use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!("helixd: daemon not yet implemented (Phase 2-4)");
    eprintln!("See shared/helix-daemon/specs/tasks.md for roadmap");
    ExitCode::from(1)
}
